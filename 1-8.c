#include <stdio.h>

int main() {
    // 変数の宣言と初期化
    int a = 10; // aに10を代入

    // 条件に基づく表示
    if (a > 0) {
        printf("a > 0");
    } else {
        printf("a <= 0");
    }

    return 0;
}
